name: CI
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: update pricedb
        run: ./update_price_db.sh
      - name: push changes
        run: |
          git config --global user.name "Dániel Kántor"
          git config --global user.email "kantord@users.noreply.github.com" 
          git add --all
          git commit -m "update prices"
          git push origin master
